# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
.eggs/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
venv/
env/
.env
.venv/

# Environment variables
.env
.env.local
.env.*.local

# Node / React
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm
frontend/build/
frontend/.env.local
frontend/.env.development.local
frontend/.env.test.local
frontend/.env.production.local

# Uploaded videos and outputs (keep folder, ignore files)
backend/uploads/*.mp4
backend/uploads/*.mov
backend/uploads/*.avi
backend/data/videos/*.mp4
backend/data/videos/*.mov
backend/data/videos/*.avi

# Raw source videos (large, not needed in repo — kept locally / on backup)
backend/data/raw_videos/
# Feature extraction cache (regenerable) and Apple Numbers spec files
backend/data/labels/features_cache.json
*.numbers
# Labels derivadas (se regeneran con train_model.py --mode cut desde cuts.csv)
backend/data/labels/labels.csv
# Backups locales del dataset piloto
backend/data/labels/_pilot_backup/

# Trained model (large binary, track separately if needed)
backend/app/models/*.joblib
backend/app/models/*.pkl

# MediaPipe model files (downloaded separately)
*.task
backend/*.task
backend/app/**/*.task

# IDE / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo
Thumbs.db

# Claude Code
.claude/

# Logs
*.log
logs/

# Test coverage
htmlcov/
.coverage
coverage.xml
